<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--First item in menu bottom nav -->
    <item android:id="@+id/action_popular"
        android:title="@string/menu_popular"
        android:icon="@drawable/ic_star"
        />

    <!--Second item in menu bottom nav-->
    <item android:id="@+id/action_my_places"
        android:title="@string/menu_my_places"
        android:icon="@drawable/ic_library_add"
        app:showAsAction="always"/>

    <!--Third item in menu bottom nav -->
    <item android:id="@+id/action_favorites"
        android:title="@string/menu_favorites"
        android:icon="@drawable/ic_thumb_up"
        app:showAsAction="always"/>


    <item android:id="@+id/action_settings"
        android:title="@string/menu_settings"
        android:icon="@drawable/ic_settings"
        app:showAsAction="always"/>

</menu>